# Curated paper results

Copy or link accepted experiment outputs here after review.

Suggested layout per run:

```text
results/
  lfw_buffalo_l_<experiment_id>/
    manifest.json
    summary.json
    report.md
    roc_curve.png
    confusion_matrix.png
    NOTES.md
```

Bulk `experiments/` outputs stay gitignored; only curated, small artifacts belong here.
